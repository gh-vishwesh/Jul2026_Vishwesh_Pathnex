Day 19 - Advanced Monitoring, Logging, and Security
🔹Ansible - Setup Prometheus for Monitoring
- name: Install Prometheus for Monitoring
  hosts: all
  become: yes
  tasks:
    - name: Install Prometheus dependencies
      yum: 
        name: 
          - prometheus
          - prometheus-node-exporter
        state: present
    - name: Start Prometheus service
      service:
        name: prometheus
        state: started
        enabled: yes
🔹Terraform - Setup AWS CloudWatch for Monitoring
resource "aws_cloudwatch_log_group" "vishwesh_log_group" {
  name = "vishwesh_log_group"
}

resource "aws_cloudwatch_log_stream" "vishwesh_log_group" {
  log_group_name = aws_cloudwatch_log_group.vishwesh_log_group.name
  name          = "vishwesh_log_group"
}

🔹Kubernetes - Deploy Flueted for Logging
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluented
spec: 
  replicas: 1
  selector:
    matchLabels:
      app: fluented
  ports:
    - protocols: TCP
      port: 24224

🔹Jenkinsfile - Integrating Prometheus Monitoring in Pipeline
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'BUilding Docker image...'
                docker.build('vishwesh-web-app')
            }
        }
        stage('Test') {
            stage {
                echo 'Running tests...'
            }
        }
        stage('Monitor Deployment') {
            steps {
                script {
                    sh 'kubectl apply -f kubernetes/deployment.yaml'
                    sh 'kubectl get pods --watch'
                }
            }
        }
    }
}

🔹 GitLab CI/CD - Prometheus and Logging Integration
stages:
  - build
  - push 
  - deploy

build:
  stage: 
  script:
    - docker build -t vishwesh-web-app

push:
  stage: push
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push vishwesh-web-app

deploy:
  stage: deploy
  script:
    - kubectl apply kubernetes/deployment.yaml
    - kubectl get pods --watch


🔹 Docker
# Flask Application (Python)
from flask import Flask
app = Flask(__name__)
@app.route('/')
def home():
    return "Hello Vishwesh"
app.run(host='0.0.0.0', port=5000)

# Docker File
FROM python:3.11
WORKDIR /opt/vishwesh/flask-app
COPY . /opt/vishwesh/flask-app/
RUN pip install flask
CMD ["python", "/opt/vishwesh/flask-app/app.py"]
